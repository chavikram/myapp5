<%= form_for(@question_detail) do |f| %>
  <% if @question_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_detail.errors.count, "error") %> prohibited this question_detail from being saved:</h2>

      <ul>
      <% @question_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :question_id %><br>
    <%= f.collection_select :question_id, Question.all,:id,:desc %>
  </div>

  <div class="field">
    <%= f.label :question_text %><br>
    <%= f.text_area :question_text, :rows => 10,:cols=>50 %>
  </div>
  <div class="field">
    <%= f.label :answer_text %><br>
    <%= f.text_area :answer_text, :rows => 10,:cols=>50 %>
  </div>
  <div class="field">
    <%= f.label :statement_text %><br>
    <%= f.text_area :statement_text, :rows => 10,:cols=>50 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
